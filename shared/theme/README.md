# Shared Theme Tokens

`tokens.json` is the canonical cross-platform theme source for web + Flutter.

## Generate artifacts

```bash
node scripts/generate-theme-artifacts.js
```

Outputs:

- `src/app/generated-theme-tokens.css`
- `flutter_app/lib/theme/generated_theme_tokens.dart`
